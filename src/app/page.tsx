
import QuizContainer from "@/components/quiz/QuizContainer";

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-4 bg-gradient-to-b from-[#eef7ff] to-[#dceefb] relative overflow-hidden">
      <QuizContainer />
    </main>
  );
}
